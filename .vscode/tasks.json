{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "just --list",
            "options": {
                "cwd": "${workspaceFolder}/2024/rust"
            },
            "problemMatcher": [],
            "label": "rust: just"
        }
    ]
}